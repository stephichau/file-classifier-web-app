import React from 'react';
import PropTypes from 'prop-types';
import SmallInfoCard from '../../../WebUI';

const Courses = ({
  classes,
}) => {

  return <div>Courses</div>;
};

Courses.defaultProps = {

};

Courses.propTypes = {
  classes: PropTypes.object.isRequired,
};

export default Courses;
